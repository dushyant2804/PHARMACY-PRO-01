# PharmacyOS local desktop data folder

Reserved for future Windows desktop package metadata and updater staging. Auto-update is not implemented yet.

Do not place the active SQLite database here unless the launcher configuration is intentionally changed. The current local launcher uses the existing database at `local_data/pharmacyos.sqlite3` to avoid touching existing local data.
